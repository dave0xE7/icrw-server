

if no account or session was found on the device
  send android device info
  #check if device is known
  create a new account based on client hash (device info)
  respond account + token
else (account found)
  send walletId, walletKey
  if walletKey matches database
    server creates new walletKey
    respond with walletId, walletKey
    return
  false



api methods
  createAccount                         #create a new account for a new user
    account: #, key: #
  checkAccount(account)                 #see if account exists
    t/f
  testAccount(account, key)             #see if key is correct
    t/f
  secureAccount(account, key)           #set a new key
    key: #

  getAddress(account, key)              #
    address: #
  getBalance(account, key)
    balance, address
  listTransactions(account, key)
        array Transactions
makeTransaction(account, key, receiver, amount)
        txid


# MAYBE ALL IN ONE function
takes account + key
